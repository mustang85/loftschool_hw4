ƒиректива - это ng-app,ng-show,ng-click,ng-model ...
ƒирективы что-то выполн€ют, тоесть это функци€ заключенна€ в тегах.(может принимать агрументы)

ng-model - работа с input,textarea - дву-направленна€ зависимость.

 онтроллер - это функци€, задача которой св€зать модель данных с представлением.

јнгул€р построен на модул€х angular.module('name module','зависимости - ngRoute,$scope ..');
зависимости дополн€ют функционал нашего модул€.

чтобы дальше работать с модулем, мы должны сохранить модуль в переменную.

прив€зываем модуль к приложению - <html lang="en" ng-app="app">

после того как модуль создан мы можем его дополн€ть контроллерами, сервисами, директивами.

app.controller('CardCtrl',['$scope', function($scope) {

}]);

$scope - область видимости, обеспечивает общую модель данных между контроллером и директивной, 
выполн€€ роль некой прослойки, в процессе линковки самого шаблона, он прив€зывет директивы 
из watch к области видимости, соот-но watch позвол€ем уведомл€ть директивы об изменени€х свойств, 
все изменени€ из watch переход€т и отображаютс€ внутри ƒќћ дерева.

контроллеры и директивы св€зываютс€ сами через $scope тоесть через саму модель данных


phoneBook
          |_  CardCtrl


factory (создание сервисов - http,scope) - что-то типо модул€ при создании самовызывающейс€ функции(модуль) с возвращение методов. 
(объекты подстановки, которые расшир€ют объекты) некий синглтон, 
одна сущность на которую ссылаютс€ при работе
